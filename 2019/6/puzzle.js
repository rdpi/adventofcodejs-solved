const readline = require('readline');
const fs = require('fs');

let input = fs.readFileSync(`${__dirname}/input`,'utf8').split('\n');

module.exports = {
  solution1: async () => {
    answer = 0;
    input.forEach((line) => {
    // solution
    });
    return answer;
  },
  
  solution2: async () => {
    let answer = 0;
    input.forEach((line) => {
    // solution
    });
    return answer;
  }
}
